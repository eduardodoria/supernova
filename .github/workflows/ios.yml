name: IOS

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: macOS-latest

    steps:
      - uses: actions/checkout@v2

      - name: Xcode version
        run: /usr/bin/xcodebuild -version
        
      - name: Build archive
        run: |
          cd workspaces/xcode/
          xcodebuild -sdk iphoneos -project Supernova.xcodeproj -list
